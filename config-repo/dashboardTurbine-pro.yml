spring:
  application:
    name: dashboard-turbine
server:
  port: 8989
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
turbine:
  appConfig: client1,client2
  aggregator:
    clusterConfig: default
  clusterNameExpression: new String("default")
