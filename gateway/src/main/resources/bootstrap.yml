spring:
  cloud:
    config:
      name: gateway
      profile: pro
      label: master
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
